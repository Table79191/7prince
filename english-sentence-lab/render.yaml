services:
  - type: web
    name: sentencelab-clause-api
    runtime: python
    rootDir: english-sentence-lab
    buildCommand: pip install -r server/requirements.txt
    startCommand: gunicorn --chdir server app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
    autoDeploy: true
